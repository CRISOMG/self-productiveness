# 1. Copia tu URL final aquí (ej: https://n8n.tu-dominio.com)
# Esta URL debe coincidir con la que configures en Cloudflare Zero Trust -> Public Hostnames
# Si usas un dominio propio, ponlo aquí.
CLOUDFLARE_TUNNEL_TOKEN=pega-tu-token-aqui
WEBHOOK_SUBDOMAIN=yourfocus
WEBHOOK_DOMAIN=dieguscl.com
WEBHOOK_URL=https://yourfocus.dieguscl.com

# N8N Encryption Key (Important: Use the same key across environments if you want to share credentials!)
# Generate one with: docker run --rm n8nio/n8n n8n encryption-key generate
N8N_ENCRYPTION_KEY=

# --- Supabase Cloud (Prod) ---
SUPABASE_CLOUD_HOST=db.tu-proyecto.supabase.co
SUPABASE_CLOUD_PASSWORD=tu_password_cloud
SUPABASE_CLOUD_PORT=5432 # or 6543
SUPABASE_CLOUD_DATABASE=postgres
SUPABASE_CLOUD_USER=postgres

# --- Supabase Local (Dev) ---
# Assuming run via 'supabase start'
SUPABASE_LOCAL_PORT=54322
SUPABASE_LOCAL_PASSWORD=postgres # Default for local
SUPABASE_LOCAL_DATABASE=postgres
SUPABASE_LOCAL_USER=postgres